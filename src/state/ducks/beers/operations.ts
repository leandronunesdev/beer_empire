import actions from './actions';

const beerOperations = {
  getBeers: actions.getBeers,
  deleteBeer: actions.deleteBeer,
  createBeer: actions.createBeer,
  updateBeer: actions.updateBeer,
};

export default beerOperations;
